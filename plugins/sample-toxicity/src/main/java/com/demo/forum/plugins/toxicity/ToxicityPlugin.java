package com.demo.forum.plugins.toxicity;

import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

public class ToxicityPlugin extends Plugin {
    public ToxicityPlugin(PluginWrapper wrapper) {
        super(wrapper);
    }
}
